<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="yebug.list">
      <field name="name">yebug list</field>
      <field name="model">yebug.yebug</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="yebug.action_window">
      <field name="name">yebug window</field>
      <field name="res_model">yebug.yebug</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="yebug.action_server">
      <field name="name">yebug server</field>
      <field name="model_id" ref="model_yebug_yebug"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="yebug" id="yebug.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="yebug.menu_1" parent="yebug.menu_root"/>
    <menuitem name="Menu 2" id="yebug.menu_2" parent="yebug.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="yebug.menu_1_list" parent="yebug.menu_1"
              action="yebug.action_window"/>
    <menuitem name="Server to list" id="yebug" parent="yebug.menu_2"
              action="yebug.action_server"/>
    -->
  </data>
</odoo>